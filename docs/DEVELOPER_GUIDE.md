# 📖 Гайд по поддержке и разработке TikClone Pro

## Структура файлов и их назначение

### 📄 HTML файл
- **`index.html`** - основной файл приложения
  - Содержит всю разметку страницы
  - Подключает все CSS и JS файлы в правильном порядке
  - Разбит на логические секции (views) для каждого экрана

### 🎨 CSS папка (`css/`)

#### `variables.css`
- Определение CSS переменных (цвета, размеры, переходы)
- Базовые скрипты для всех элементов
- Тёмная и светлая тема

#### `layout.css`
- Основной контейнер приложения
- Система виртуальных экранов (views)
- Основные анимации

#### `navigation.css`
- Стили нижней навигационной панели
- Кнопка загрузки видео
- Значки уведомлений

#### `feed.css`
- Контейнер ленты с видео
- Индикатор pull-to-refresh
- Анимация скелетона загрузки
- Фильтры видео
- Прогресс-бар видео

#### `video-overlay.css`
- Оверлей поверх видео (информация об авторе, описание)
- Боковая панель с кнопками действий (лайки, комментарии, шэр)
- Аватар и система подписок
- Хэштеги

#### `search.css`
- Стили поисковой строки
- Фокус и взаимодействие

#### `auth.css`
- Экран входа
- Формы для ввода email и пароля
- Кнопка входа с анимацией

#### `upload.css`
- Экран загрузки видео
- Область для drag-and-drop
- Превью камеры
- Кнопка записи с анимацией
- Выбор фильтров
- Поля для описания и хэштегов

#### `profile.css`
- Заголовок с аватаром и статистикой
- Сетка видео (3 колонки)
- Интерактивные элементы профиля

#### `comments.css`
- Нижний лист (bottom sheet) с комментариями
- Список комментариев с аватарами
- Поле ввода комментария
- Анимации при загрузке комментариев

#### `components.css`
- Модальное окно шэр
- Переключатель темы
- Toast уведомления
- Лоадер
- FAB (плавающая кнопка действий)

### 🔧 JavaScript папка (`js/`)

#### `data-service.js` - Класс `AdvancedDataService`
**Ответственность**: Управление данными приложения

**Основные методы:**
- `init()` - инициализация, загрузка данных из хранилища
- `getFeed(page, limit)` - получить ленту видео с пагинацией
- `uploadVideo(file, metadata)` - загрузить новое видео
- `toggleLike(videoId)` - переключить лайк
- `addComment(videoId, text)` - добавить комментарий
- `searchVideos(query)` - поиск по описанию и хэштегам
- `login(email, password)` - вход в систему
- `logout()` - выход из системы

**Свойства:**
- `filters` - массив доступных фильтров
- `userVideos` - массив всех видео в системе
- `settings` - настройки пользователя

#### `view-renderer.js` - Класс `AdvancedViewRenderer`
**Ответственность**: Создание и форматирование UI компонентов

**Основные методы:**
- `createVideoCard(video, options)` - создать карточку видео
- `renderComments(comments)` - создать HTML списка комментариев
- `renderShareOptions(videoId)` - создать опции шэра
- `renderFilterOptions(filters)` - создать выбор фильтров
- `formatNumber(num)` - форматировать большие числа (1.2M, 850K)
- `formatTimeAgo(timestamp)` - форматировать время ("1 мин назад")
- `showToast(msg, type)` - показать уведомление
- `showLoading()` / `hideLoading()` - показать/скрыть лоадер

#### `app.js` - Класс `AdvancedApp`
**Ответственность**: Основное приложение, управление событиями

**Основные методы:**
- `init()` - инициализация при загрузке
- `setupEventListeners()` - подключение обработчиков событий
- `loadFeed(clear)` - загрузить ленту видео
- `attachVideoEvents()` - подключить события к видео
- `navigateTo(viewId)` - переключиться на другой экран
- `openComments(videoId)` - открыть комментарии
- `sendComment()` - отправить комментарий
- `showShareModal(videoId)` - показать модальное окно шэра
- `toggleCamera()` - включить/выключить камеру
- `startRecording()` / `stopRecording()` - запись видео
- `updateProfileUI()` - обновить UI профиля

**Основное состояние (state):**
```javascript
state = {
    currentVideoId: null,           // ID текущего видео
    activeCommentsVideoId: null,    // ID видео с открытыми комментариями
    currentPage: 0,                 // Текущая страница ленты
    loading: false,                 // Загружается ли лента
    hasMore: true,                  // Есть ли ещё видео
    isRecording: false,             // Идёт ли запись видео
    mediaRecorder: null,            // Объект MediaRecorder
    recordedChunks: [],             // Куски записанного видео
    selectedFilter: 'none',         // Выбранный фильтр
    theme: 'dark'                   // Текущая тема
}
```

## 🔄 Поток данных

```
User Action (click, input)
         ↓
Event Listener (в app.js)
         ↓
DataService Method (изменение данных)
         ↓
ViewRenderer.showToast() (обратная связь)
         ↓
DOM Update (updateProfileUI, attachVideoEvents и т.д.)
```

## 🎯 Как добавить новую функцию

### Пример: Добавить функцию "Избранное" (Like для сохранения)

**Шаг 1**: Добавить метод в `DataService` (`js/data-service.js`)
```javascript
toggleFavorite(videoId) {
    const video = this.userVideos.find(v => v.id === videoId);
    if (video) {
        video.isFavorite = !video.isFavorite;
        localStorage.setItem(this.STORAGE_KEY, JSON.stringify(this.userVideos));
        return video.isFavorite;
    }
    return false;
}

getFavoriteVideos() {
    return this.userVideos.filter(v => v.isFavorite);
}
```

**Шаг 2**: Добавить кнопку в HTML (`index.html`)
```html
<div class="action-btn favorite-btn" data-id="${video.id}" title="Добавить в избранное">
    <svg viewBox="0 0 24 24">
        <!-- SVG иконка закладки -->
    </svg>
    <span>Избранное</span>
</div>
```

**Шаг 3**: Добавить CSS (`css/video-overlay.css`)
```css
.action-btn.favorite-btn svg { fill: #fff; }
.action-btn.favorite-btn.active svg { fill: var(--accent-secondary); }
```

**Шаг 4**: Добавить обработчик события в `app.js`
```javascript
const favoriteBtn = item.querySelector('.favorite-btn');
favoriteBtn.addEventListener('click', (e) => {
    e.stopPropagation();
    const isFavorite = this.dataService.toggleFavorite(parseInt(videoId));
    favoriteBtn.classList.toggle('active', isFavorite);
    AdvancedViewRenderer.showToast(isFavorite ? '⭐ Добавлено в избранное' : '⭐ Удалено из избранного', 'success');
});
```

## 🚨 Отладка

### Как смотреть данные в консоли:
```javascript
// В консоли браузера:
app.dataService.userVideos // Все видео
app.state // Состояние приложения
localStorage // Все сохранённые данные
```

### Проверка ошибок:
- Откройте DevTools (F12)
- Смотрите вкладку "Console"
- Смотрите вкладку "Application" → LocalStorage

## 📦 Зависимости

**Нет внешних зависимостей** - полностью на Vanilla JavaScript!

## 🔄 Цикл разработки

1. **Создание идеи** → Описание функции
2. **Дизайн** → Добавить CSS
3. **HTML** → Добавить разметку
4. **DataService** → Добавить логику данных
5. **ViewRenderer** → Добавить рендеринг (если нужно)
6. **App** → Добавить события и логику
7. **Тестирование** → Проверить в браузере

## 💾 Сохранение данных

При каждом изменении (лайк, комментарий, загрузка):
```javascript
localStorage.setItem(this.STORAGE_KEY, JSON.stringify(this.userVideos));
```

Данные JSON хранятся в браузере локально.

## 🎬 Запись видео

Использует `MediaRecorder API`:
1. `navigator.mediaDevices.getUserMedia()` - доступ к камере
2. `new MediaRecorder()` - создание записи
3. `mediaRecorder.start()` - начало записи
4. `mediaRecorder.stop()` - завершение
5. `FileReader.readAsDataURL()` - преобразование в Data URL

## 🌍 Для Production

Нужно заменить:
- **LocalStorage** → Реальную БД (Firebase, PostgreSQL, MongoDB)
- **Загрузка видео** → Облачное хранилище (AWS S3, Google Cloud)
- **Аутентификация** → Сервер с JWT токенами
- **HTTPS** → Обязательно

---

**Happy coding!** 🚀
